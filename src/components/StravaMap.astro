---
import { fetchStravaActivities } from "../lib/strava";
import MapWithPaths from "./StravaMap.tsx";

const stravaToken = Astro.cookies.get("strava_token")?.value;
console.log("the token is", stravaToken);
const activities = await fetchStravaActivities(stravaToken);
---

<MapWithPaths client:only="react" {activities} />
